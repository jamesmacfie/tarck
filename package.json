{
  "name": "tarck",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:wrangler": "astro build && wrangler pages dev dist",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "preview:wrangler": "astro build && wrangler pages dev dist",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.0.0"
  }
}
